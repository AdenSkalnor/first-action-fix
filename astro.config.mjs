// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  // Replace with your actual custom domain when ready.
  // Until then, comment this out and use the base option below instead.
  site: 'https://YOURCUSTOMDOMAIN.com',

  // If using github.io/first-action-fix (no custom domain), uncomment this:
  // site: 'https://YOURUSERNAME.github.io',
  // base: '/first-action-fix',
});
